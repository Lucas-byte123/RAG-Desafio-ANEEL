# RAG ANEEL — dependencias congeladas (testado em Python 3.11+)
# Para deploy em Oracle Compute (E5.Flex ou A1.Flex)

# Oracle (banco vetorial 23ai + cliente OCI Generative AI)
oracledb==3.4.2
oci==2.172.0

# UI
streamlit==1.56.0

# Reranking local (bge-reranker-v2-m3 — multilingue SOTA)
sentence-transformers==5.4.1
torch==2.11.0
transformers==5.6.2

# Extracao de PDFs (pipeline de ingestao)
PyMuPDF==1.27.2.3
pdfplumber==0.11.9

# Util
numpy==2.4.4
requests==2.33.1
tqdm==4.67.3

# Health endpoint HTTP (opcional, leve)
fastapi==0.121.0
uvicorn==0.41.0
